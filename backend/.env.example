# FinChat Backend Configuration
# Copy this file to .env and fill in your values

# Server
PORT=3000
NODE_ENV=development

# JWT Secret — change this to a long random string in production
JWT_SECRET=finchat_super_secret_jwt_key_change_this_in_production_2024

# SQLite database file path
DB_PATH=./finchat.db

# IPFS via Pinata (get free API keys at https://pinata.cloud)
PINATA_API_KEY=your_pinata_api_key_here
PINATA_SECRET_KEY=your_pinata_secret_key_here
PINATA_GATEWAY=https://gateway.pinata.cloud/ipfs

# Solana (devnet = free, no setup needed)
SOLANA_NETWORK=devnet
SOLANA_RPC=https://api.devnet.solana.com

# Qwen / Ollama local fraud detection
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:3b

# File uploads
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=10

# CORS — frontend origin
FRONTEND_URL=http://localhost:5500
